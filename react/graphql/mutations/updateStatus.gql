mutation UpdateStatus($id: ID!, $status: SubscriptionStatus!) {
  updateStatus(subscriptionsGroupId: $id, status: $status)
    @context(provider: "vtex.subscriptions-graphql") {
    cacheId
    status
  }
}
