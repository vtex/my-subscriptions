version: 0.2
phases:
  install:
    commands:
      - yarn
  pre_build:
    commands:
      - echo $CODEBUILD_SOURCE_VERSION

      - echo $CODEBUILD_WEBHOOK_TRIGGER
      - yarn danger ci
cache:
  paths:
    - "node_modules/**/*"
