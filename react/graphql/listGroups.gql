query ListSubscriptionsGroups($statusList: [SubscriptionStatus]) {
  groups: customerSubscriptionsGroupList(statusList: $statusList)
    @context(provider: "vtex.subscriptions-graphql") {
    id
    cacheId
    status
    name
    nextPurchaseDate
    lastUpdate
    subscriptions {
      sku {
        imageUrl
        name
        detailUrl
        productName
      }
    }
    plan {
      frequency {
        periodicity
        interval
      }
      purchaseDay
    }
  }
}
