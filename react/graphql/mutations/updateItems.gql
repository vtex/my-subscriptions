mutation UpdateItemsOnSubscription(
  $subscriptionId: ID!
  $items: [ItemInput!]!
) {
  updateItems(id: $subscriptionId, items: $items)
    @context(provider: "vtex.subscriptions-graphql") {
    cacheId
    totals {
      id
      value
    }
    subscriptions {
      id
      quantity
      sku {
        id
        imageUrl
        name
        detailUrl
        productName
        variations
        measurementUnit
      }
      currentPrice
    }
  }
}
